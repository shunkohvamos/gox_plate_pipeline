{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Generate launch.json from data",
      "type": "shell",
      "command": "uv run python scripts/generate_launch_json.py",
      "options": {
        "cwd": "${workspaceFolder}",
        "env": {
          "PYTHONPATH": "${workspaceFolder}/src"
        }
      },
      "group": "none",
      "presentation": {
        "reveal": "silent",
        "panel": "shared"
      }
    }
  ]
}